# Runtime dependencies. Vercel installs exactly this file, so keep it lean —
# anything added here ships inside the serverless function bundle.
fastapi==0.141.1
uvicorn==0.53.0
SQLAlchemy==2.0.54
psycopg2-binary==2.9.13
python-dotenv==1.2.3
google-genai==2.24.0
